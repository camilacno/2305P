{
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.25.2",
    "turbo": "^1.5.5"
  },
  "dependencies": {
    "@ignite-ui/docs": "file:packages/docs",
    "@ignite-ui/react": "file:packages/react",
    "@ignite-ui/tokens": "file:packages/tokens"
  }
}
